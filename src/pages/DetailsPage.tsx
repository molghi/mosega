import Details from "../components/Details";

const DetailsPage = () => {
    return (
        <>
            {/* Object.keys(details).length > 0 */}
            <Details />
        </>
    );
};

export default DetailsPage;
